# Cloud Run / production environment variables
# Copy to .env.production and set values before deploy

NODE_ENV=production
PORT=8080

# JWT: use a long random string in production
JWT_SECRET=your-secure-random-jwt-secret

# Gemini AI (required for AI evaluation)
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-1.5-flash

# Paths (Cloud Run ephemeral storage)
DATABASE_PATH=/tmp/database.sqlite
UPLOAD_DIR=/tmp/uploads

# Optional: CORS / frontend URL (not needed when serving SPA from same origin)
# FRONTEND_URL=https://your-cloud-run-url.run.app
